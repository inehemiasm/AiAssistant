<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Include only preferences; other domains like 'file' are excluded by default when an include is present -->
    <include
        domain="sharedpref"
        path="." />
</full-backup-content>
